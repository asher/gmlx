# Serving architecture

This page has moved to the contributor internals:
[internals/serving-architecture.md](internals/serving-architecture.md).

It covers the request path from the HTTP layer through the resolver, the batch scheduler and the loader. This stub stays so that published links keep resolving.
