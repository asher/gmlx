# Adapter serving

This page has moved. Serving one quantized base with many LoRA adapters,
including the config shape, the `<id>-base` rule and the cost of an extra
adapter, is now the second half of the LoRA guide:

[lora.md, Serving one base with many adapters](lora.md#serving-one-base-with-many-adapters)

The training half of the same guide covers producing the adapter in the
first place. This stub stays so that published links keep resolving.
